import {ChangeDetectorPipe} from './change-detector-pipe';

describe('ChangeDetectorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeDetectorPipe();
    expect(pipe).toBeTruthy();
  });
});
